datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model Sensors {
  id        Int     @id @default(autoincrement())
  temp      Float
  alt       Float
  press     Float
  heat      Float
  humid     Float
  lux       Float
  flame     Int
  createdAt DateTime @updatedAt
}